# <ROLE> agent memory

<!-- Per-agent memory block. Copy this directory to
     .claude/agent-memory/<role>/ and rename ROLE.
     This is the agent's OWN scope; general project knowledge still goes
     to .claude/memory/. Keep entries durable, not session state. -->

## Conventions

<!-- Rules this agent must always follow in its domain. -->
- <convention the agent should never violate>

## Known pitfalls

<!-- Mistakes already made / traps in this domain — write them once,
     never hit them twice. -->
- <pitfall and how to avoid it>

## Decisions

<!-- Domain decisions and their reasons, so they are not re-litigated. -->
- <decision> — <why>
