/** Empty placeholder — Phase 2 (MN-34) fills this in: banks, tree, file
 *  view, chunk-boundary overlay, reindex. */
export default function MemoryPage() {
  return (
    <div style={{ padding: 16, color: "var(--fg-mute)" }}>Памʼять — Phase 2</div>
  );
}
