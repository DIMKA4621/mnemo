/** Empty placeholder — Phase 3 (MN-35) fills this in: filters, event list,
 *  detail pane, live push, deep-link into Памʼять. */
export default function JournalPage() {
  return (
    <div style={{ padding: 16, color: "var(--fg-mute)" }}>Журнал — Phase 3</div>
  );
}
