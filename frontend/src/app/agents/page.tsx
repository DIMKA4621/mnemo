/** "Coming soon" stub — the Agents feature (MN-32's own later scope,
 *  `docs/Agents-v1-scope.md`) has no shipped content yet. The route exists
 *  now so adding the real page later is content, not plumbing. */
export default function AgentsPage() {
  return (
    <div style={{ padding: 16, color: "var(--fg-mute)" }}>Агенти — скоро</div>
  );
}
