.idea
# The index DB now lives at user scope (~/.claude/mnemo/state/),
# never inside the repo. These guards remain in case of a local --root run.
memory.db
memory.db-shm
memory.db-wal
*.db

# Python
.venv/
venv/
__pycache__/
*.py[cod]

# Local embedding model / ONNX cache
.fastembed_cache/
*.onnx

# Generated by `mnemo init` — carries a literal per-bank token.
.mcp.json

# mnemo — generated wiring holds a per-bank token
.mcp.env
