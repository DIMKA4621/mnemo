# The index DB now lives at user scope (~/.claude/memory-poc/state/),
# never inside the repo. These guards remain in case of a local --root run.
memory.db
memory.db-shm
memory.db-wal
*.db

# Python
.venv/
venv/
__pycache__/
*.py[cod]

# Local embedding model / ONNX cache
.fastembed_cache/
*.onnx
