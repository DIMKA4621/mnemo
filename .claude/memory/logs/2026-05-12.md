# Day log — 2026-05-12

## Done
- Викотили міграцію, що додає колонку `users.last_seen_at`. Зробили в два
  кроки: спершу nullable + бекфіл батчами, потім NOT NULL окремим релізом.
- A non-concurrent index creation locked the users table for ~40s on
  staging. Switched to `CREATE INDEX CONCURRENTLY` and re-ran.

## Decisions
- Усі майбутні індекси на проді — тільки CONCURRENTLY, без винятків.

## Commits
- `feat: add users.last_seen_at (nullable, backfilled)`
- `perf: create last_seen_at index concurrently`
