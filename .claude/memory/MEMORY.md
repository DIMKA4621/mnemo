# mnemo project memory

Bank root: `.claude/memory` (this folder), searchable through the `mnemo` MCP
server — the `search` tool. This file is an **index**: links + quick facts only;
detail lives in `topics/`, day notes in `logs/`.

## Architecture

- [Поверхні служби](topics/service-surfaces.md) — чотири обличчя, розділені
  токеном: приватний `/api/*`, **`/mcp?token=<токен банку>` без сегмента**
  (токен визначає банк і більше ніщо), `/mcp-admin` під сервісним токеном,
  `/mcp-tools/*` як байт-у-байт дзеркало для людини. Там же: токен на банк,
  `bank_id` виводиться а токен видається (вада з `_cancelled`),
  багатоклієнтність як наслідок `stateless_http`, і чому `MCPServer` у SDK 2.0
  тримається на монтуванні під час імпорту.
- [Проводка проєкту](topics/project-wiring.md) — що `init` кладе в чужий
  репозиторій: ключ `mnemo-memory` (старий `mnemo` **перейменовується, не
  доповнюється**), `.mcp.json` генерований і gitignored проти шару
  template + `.mcp.env` + `mcp-setup.*`, **динамічні** підстановки (відсутня
  змінна = помилка, а не дослівний `{{TOKEN}}` при `exit 0`), оновлення
  правила за sha256, `git check-ignore -v` після seed (broad `.claude` більше
  не ховає memory мовчки) і чому `edited` ≠ «правили руками». Хуків немає.
- [Встановлення й життєвий цикл](topics/install-lifecycle.md) — два рівні
  (інсталятор = машина, `init` = тека), знесення як дзеркало, осиротілі
  індекси, ознака v2 і чому список v2-проєктів не відновлюється. Головне:
  **оновлення на місці не перевіряє обіцянку «клонуй і постав»** — звідси
  піновані мажори (`mcp>=2.0,<3`, рішення #29).
- [Кабінет](topics/cabinet-ui.md) — вибір теки обходить ФС на боці бекенда
  (`webkitdirectory` шляху не дає), усі дії банку в одному меню `···`,
  налаштування як **екран, а не модалка** (затемнений фон показував би
  застарілий стан), окремий warning lane для `REBUILD PENDING` і structured
  «Обслуговування»: CLI/UI читають один doctor-report, cleanup приймає лише
  щойно показані orphan id. **Нова оболонка реалізована** у
  `src/webui/static/` (сайдбар = три маршрути, хедер = сторінка, підвал
  сайдбару = машина) з `docs/cabinet-design.html` як еталоном; `app.js`
  розбитий на `shell.js`/`page-{memory,journal,settings}.js`, CSS — на
  `styles/*.css`. Journal snapshot-текст, кнопка завантаження моделі й
  чекбокс `init` у діалозі банку **зроблені** 2026-08-20 (друге) —
  `logs/2026-08-20-cabinet-followups.md`. Не зроблено: фільтр статусу й
  повнотекстовий пошук журналу.
- [Native Windows support](topics/windows-native-support.md) — PowerShell 5.1
  installer, canonical launcher contract, portable wiring, and verification.
- [Пропускна здатність ембедингу](topics/embedding-throughput.md) — наш
  CPU-резидент проти bge-m3 на GPU через Ollama, на однакових 35 571 токені:
  **8.8× на пакеті, але лише 1.8× на одному запиті**. Головна знахідка:
  **час індексації = паддед-токени / 243** — але це властивість **CPU**-
  бекенда, на GPU домінує кількість викликів. Звідси реалізоване
  `index.plan_batches` (**≈1.6×** на перебудові) і те, що бюджет батча
  **мусив стати по-провайдерним**: виграшне CPU-число робить GPU **удвічі
  повільнішою**, і помилка мовчазна.
  Там же — чому `CHUNK_CAPACITY` тримає максимум під вікном e5 у 512
  токенів, і recall трьох пар (модель × рантайм): **усі три однакові, бо
  `test_search.py` — поріг регресії, а не інструмент порівняння**.
- [Якість пошуку: вади й черга робіт](topics/search-quality.md) — **лексична
  нога пошуку була мертва** (`_fts_escape` робила з запиту фразу → 0 хітів на
  будь-якому питанні). Виправлено, і `tests/eval_search.py` це довів: арма зі
  старим кодом збігається з чистим вектором **у всіх шести метриках**. Рівна
  вага при цьому шкодить — правильна роль лексики це **переранжування
  векторних кандидатів**, вага ~6:1. Там же: стеля чанка в символах не
  обмежує токени, 5% чанків — голі заголовки, справжня причина «плаваючого
  404» від MCP, порядок робіт і що вирішено НЕ робити.
- [Машинні налаштування і другий провайдер](topics/provider-settings.md) —
  `state/settings.json` **реалізовано** (`src/settings.py`, `GET`/`PUT
  /api/settings`, контракт §6.6): env > файл > дефолт, і кожне значення
  несе своє `source`, бо збережене може бути інертним. Усе віддає **функція**
  — константа при імпорті тут не працює, і ця вада вже лежала в
  `providers/api.py`. Там же: Ollama як конфігурація `api`, чому зміна
  провайдера **завжди** тягне перебудову (збіг `dim` — найнебезпечніший
  випадок, підтверджено живцем), керування резидентом, і чому Docker поки ні.
- [Відкладене й черга робіт](topics/deferred.md) — що вирішено зробити пізніше
  і чому саме в такому порядку; там же дрібні вади наскрізної перевірки й
  рішення, які лишились за користувачем.

## Quick facts

- Памʼять цього проєкту **переїхала в репозиторій** (`.claude/memory/`) з
  `~/.claude/projects/E--work-projects-other-mnemo/memory/`. Наслідок:
  нативного автозавантаження `MEMORY.md` більше немає — памʼять знаходиться
  **тільки пошуком** через MCP. Стара тека лишилась **застиглим бекапом**.
- Тули звуться `search`, `tree`, `reindex` — **без префікса** `memory_`: клієнт
  і так подає їх як `mcp__<server>__<tool>`.
- **Чим міряти якість пошуку:** `tests/eval_search.py` — 64 кейси на цьому
  банку, кожна нога окремо. Розмітка — **дослівна фраза, не шлях**: переживає
  перечанкування й не гниє мовчки. Не `test_*` навмисно (потребує моделі й
  саме цієї памʼяті). База: vector 0.51 MRR / 0.75 r@10; краще сьогодні —
  0.54 / 0.81. `test_search.py` лишається порогом регресії, не інструментом
  порівняння.
- Індекс зветься `sha1(корінь)[:16].db`; звʼязок односторонній, тож зняті банки
  лишають осиротілі файли. `src/diagnostics.py` — **одне** structured джерело
  для CLI doctor й кабінету; cleanup явний і приймає тільки показані id,
  перечитуючи registry двічі. Автоматично — ніколи.
- **Стан банку — одне поле `state`** (`enabled|frozen|disabled`), не пара
  булеанів: `banks.json` редагують руками, а два поля про один факт розходяться.
  `frozen` — не стежимо, **але шукається**. `Bank.enabled` лишилась
  **обчислюваною** property (плюс `watched` / `searchable`), тож стан задається
  тільки через `state`.
- **Ollama: `keep_alive` працює лише на нативному `/api/embed`.** На
  OpenAI-сумісному `/v1/embeddings` поле приймається й **мовчки ігнорується**
  (виміряно). Будь-яка перевірка «чи модель у пам'яті» — `GET /api/ps`, не
  припущення з коду відповіді.

## Logs

- [2026-08-20 (п'яте)](logs/2026-08-20-init-auto-mcp-setup.md) — `mnemo init`
  тепер сам запускає `mcp-setup.sh`/`.ps1` останнім кроком (`_run_setup_script`,
  `scaffold.py`), замість друкувати «run it yourself» — `.mcp.json` готовий
  без ручної команди. Невдача (зламаний `edited`-скрипт) не валить `init`,
  лише фолбекає на стару інструкцію. Перші наскрізні тести `init_project()` в
  суїті; безпечний спосіб тестувати його без ризику підняти реальний сервіс —
  мокати `src.client.Client` цілком, не просто вказувати недосяжний порт.
- [2026-08-20 (четверте)](logs/2026-08-20-embed-wording-journal-polish.md) —
  особисті GPU-заміри (8.8×, RX 580) прибрано з **глобального** UI-тексту
  (`presets.py`'s Ollama note, rebuild-діалог) — не замінені на «правильне»
  число, просто прибрано, бо особисте залізо не годиться для обіцянки нікому.
  «Пам'ять» бекенда → «Оперативна памʼять»; badge `unloaded` «не завантажена»
  → «не в памʼяті» (те саме слово, що й у кнопки «Завантажити» на диск, дві
  дії одним словом читались як «модель видалили?»). Журнал: snapshot-текст
  ховає деталі за 2 реченнями з розгортанням у тому ж блоці, статус — `.badge`
  пігулка замість крапки+слова (блимало в купу з банком/тригером/часом),
  «Відкрити в Памʼяті» → «Відкрити файл» звичайною обведеною кнопкою (була
  `btn-ghost` — прозора рамка, тому виглядала текстом), і той самий
  `.pane-resizer` тепер і між списком подій і деталями. Спільний
  `wireColumnResizer()` виніс у `app.js`, Памʼять переписана на нього ж.
- [2026-08-20 (третє)](logs/2026-08-20-settings-general-pane-resize.md) —
  Settings-розділ «Служба» → «Загальні», перенесений першим (тема кабінету →
  автозапуск → стан процесу); тема отримала `.set-toggle`, якого їй бракувало
  (без нього `.segmented` розтягується на всю ширину поля). Памʼять: три
  заголовки колонок (Банки/Файли/Вміст) вирівняні до однакової `height`
  (раніше — різна `min-height` через різний вміст); Банки/Файли тепер
  тягнуться мишкою (`.pane-resizer`), збережено в
  `localStorage['mnemo_pane_widths']`.
- [2026-08-17](logs/2026-08-17-cabinet-parity.md) — крок D: окремий
  `REBUILD PENDING` banner + масовий existing-reindex, structured doctor для
  CLI/UI, explicit orphan cleanup лише за показаними id, hot settings без
  фальшивого рестарту, hosted endpoint probe і гучний `git check-ignore`
  warning. **Пастка:** verbose git показує й negation; непорожній stdout ≠
  ignored.
- [2026-08-16 (третє)](logs/2026-08-16-embed-memory.md) — віддати пам'ять
  бекенда кнопкою (`mnemo embed`, `/api/embed/*`, `src/embedctl.py`). **Не
  вимикач:** модель повертається на першому пошуку за ~7–8 с — той самий
  компроміс, який `MNEMO_EMBED_IDLE_TIMEOUT = 0` віддав команді, а не таймеру
  (тому `0` лишається). **Знахідка:** OpenAI-сумісний `/v1/embeddings` в
  Ollama **приймає `keep_alive` і мовчки ігнорує** — тож вивантаження йде
  нативним `/api/embed`, повз `ApiProvider`, і чіпає **лише нашу** модель.
  **Урок:** «я пам'ятаю, що цього немає» — не пошук: `stop_resident()` уже
  існував.
- [2026-08-16 (друге)](logs/2026-08-16-bank-states.md) — три стани банку
  (`enabled|frozen|disabled`, **одне** поле, старий булеан читається й зникає).
  `frozen` = не стежимо, **але шукається** — і саме тому знадобилась нова
  відмова **`bank_stale` (409)**: заморожений індекс переживає зміну моделі, а
  при **однаковій ширині** ніщо не кидає й видача стає беззмістовною. Явний
  реіндекс замороженого банку виконується (черга розрізняє за `trigger`).
  **Урок про UI:** доданий бейдж — це зміна layout'у, а не тексту.
- [2026-08-16](logs/2026-08-16-settings-sections.md) — екран налаштувань дістав
  ліву навігацію (`SETTINGS_SECTIONS`), автозапуск став контролом
  (`GET`/`POST /api/autostart`, окремий ендпоінт бо `schtasks` коштує ~45 мс).
  **Урок:** перевірка «нічого не змінилось» мусить брати базову лінію **зовні
  й до** першого виклику — інакше вона міряє узгодженість *після* побічного
  ефекту й на зламаному коді проходить.
- [2026-08-15](logs/2026-08-15-release-check.md) — наскрізна перевірка перед
  випуском (`local` → Ollama → OpenAI → `local`). Знайдено **ваду
  розмірності**: `workqueue` перебудовував vec0-таблицю **старої** ширини, тож
  будь-який провайдер не на 1024 лишав банк порожнім. Якість пошуку однакова
  на трьох моделях — вибір бекенда це швидкість і приватність, не якість.
- [2026-08-14](logs/2026-08-14-router-404.md) — маршрут, якого немає, віддає
  **порожнє тіло**: конверт помилки ламав OAuth-дискавері MCP-клієнта і ховав
  справжній 401. Тест перевірено **на регресії**.
- [2026-08-13 (п'яте)](logs/2026-08-13-calibration-abandoned.md) —
  калібрування `pad_budget` **написано й викинуто**: оптимуму всередині одного
  бекенда немає (плато 8×, ефект ≤7%, розкид зондів 27%). **Урок: спершу питай,
  чи оптимум існує і чи більший за шум.**
- [2026-08-13 (четверте)](logs/2026-08-13-settings-ui.md) — сторінка
  налаштувань і кінець «`local` — єдиний»: `doctor`/`status`/`warmup` більше не
  звітують про модель під `api`, а ендпоінт **показують, не викликаючи**.
- [2026-08-13 (третє)](logs/2026-08-13-presets.md) — довідник моделей
  (`src/presets.py`). Дірка з `passage:`/`query:` закрита й має число —
  косинус **0.9481** між префіксованим і голим ембедингом того самого тексту.
- [2026-08-13 (друге)](logs/2026-08-13-settings.md) — `state/settings.json`:
  env > файл > дефолт. Пастка з імпорт-константами **вже лежала в коді**; тест
  мусить бути в підпроцесі, інакше зелений на зламаному.
- [2026-08-13](logs/2026-08-13-batching.md) — батчування за паддед-вартістю
  (**≈1.6×**). Ручку довелося зробити **по-провайдерною**: краще CPU-число дає
  GPU 0.50×, і це видно лише замiром.
- [2026-08-12 (сьоме)](logs/2026-08-12-search.md) — пошук: лексична нога була
  мертва, ваги в RRF **6:1** на користь вектора, а «різати за токенами»
  реалізували, заміряли й **відхилили**. Індекс почав писати `chunker_key`.
- [2026-08-12 (шосте)](logs/2026-08-12-theme.md) — світла/темна тема; заразом
  знайдено, що `.btn-danger`/`.btn-primary` **завжди** програвали каскад
  загальному `.btn` через порядок правил у файлі.
- [2026-08-12 (пʼяте)](logs/2026-08-12-installers.md) — `install.sh` /
  `uninstall.sh` **вперше виконуються** в CI; ще до прогону знайдено
  `uninstall.sh` з режимом `100644` — у diff це невидимо.
- [2026-08-12 (четверте)](logs/2026-08-12-refresh.md) — `init` **оновлює** свій
  `mcp-setup.*`, а не лише створює. Перша версія лежала в гілці bootstrap і не
  діставала жодного вже підключеного проєкту — **тест був хибний так само, як
  код**.
- [2026-08-12 (третє)](logs/2026-08-12-pipeline.md) — `tests/test_pipeline.py`
  **без моделі**, на хеш-провайдері. Першим же прогоном знайшов, що на
  python.org-збірці macOS mnemo не працює взагалі: Python без завантажуваних
  розширень SQLite → `sqlite-vec` не вантажиться, хоч `import` і проходить.
- [2026-08-12 (друге)](logs/2026-08-12-ci.md) — CI був червоний з першого
  прогону, причин **три**; серед них: `bash` на Windows-ранері це лаунчер WSL,
  і дві перевірки проходили вхолосту.
- [2026-08-12](logs/2026-08-12-docs.md) — доки наздогнали код; README
  переписано як вітрину, скіл звужено до суджень.
- [2026-08-11 (друге)](logs/2026-08-11-migration.md) — міграція v2→v3:
  інсталятор бачить v2, `doctor` шукає осиротілі проєкти, правило оновлюється
  за хешем; знайдено вічне скасування черги.
- [2026-08-11](logs/2026-08-11.md) — ключ MCP-запису став `mnemo-memory`;
  старий перейменовується, а не доповнюється.
- [2026-08-10 (пʼяте)](logs/2026-08-10-uninstall.md) — деінсталятор і повний
  цикл «знести → поставити з нуля»; знайдено зламану свіжу інсталяцію (`mcp`
  2.0) і брехливий фінал інсталятора (`start` не чекав на `/health`).
- [2026-08-10 (четверте)](logs/2026-08-10-hooks.md) — хуки видалено зовсім;
  перевірено двома живими сесіями **через журнал, а не самозвітом**.
- [2026-08-10 (третє)](logs/2026-08-10-install.md) — встановлення з нуля однією
  командою: інсталятор доводить до `doctor`; модель за питанням.
- [2026-08-10 (друге)](logs/2026-08-10-cli.md) — резолв банку **в обидва боки**
  (з кореня проєкту нічого не працювало); `--help` із описами.
- [2026-08-10](logs/2026-08-10.md) — осиротілі індекси: `doctor` рахує,
  `clean-orphans` прибирає; `mode=ro` **не** означає «нічого не чіпає».
- [2026-07-31](logs/2026-07-31.md) — **токен став адресою банку** (`/mcp` без
  сегмента); токен на банк; обличчя `/mcp-admin`; `.mcp.json` gitignored.
- [2026-07-30](logs/2026-07-30.md) — кнопка «＋ додати банк» + браузер тек;
  поворот на MCP-first: дзеркало `/mcp-tools`, переїзд памʼяті в репо.
- [2026-07-22](logs/2026-07-22.md)
